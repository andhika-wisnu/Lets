<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andhika.lets">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_launcher3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_launcher3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddRutinitas"></activity>
        <activity android:name=".EditAccount" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".StatusActivity" />
        <activity android:name=".TerjadwalActivity" />
        <activity android:name=".RutinActivity" />
        <activity android:name=".PinjamActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>